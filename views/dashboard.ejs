<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>EduFlex • Register</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script defer src="https://unpkg.com/feather-icons"></script>

  <link rel="stylesheet" href="/style/home.css" />
  <link rel="stylesheet" href="/style/instructor.css" />
</head>

<body>
  <%- include('partial/_nav') %>

  <main class="container py-4 py-lg-5">


    <div class="row g-3 g-md-4">
      <div class="col-12 col-md-4">
        <div class="course-card p-3 p-xl-4">
          <div class="d-flex align-items-center justify-content-between">
            <span class="text-muted">Courses</span>
            <i data-feather="book-open" width="18"></i>
          </div>
          <div class="display-6 fw-bold mt-2"><%= totalCourses %></div>
          <div class="text-muted">Published: <%= publishedCourses %></div>
        </div>
      </div>
      <div class="col-12 col-md-4">
        <div class="course-card p-3 p-xl-4">
          <div class="d-flex align-items-center justify-content-between">
            <span class="text-muted">Total Students</span>
            <i data-feather="users" width="18"></i>
          </div>
          <div class="display-6 fw-bold mt-2"><%= totalCourses %></div>
          <div class="text-muted">Active this week</div>
        </div>
      </div>
      <div class="col-12 col-md-4">
        <div class="course-card p-3 p-xl-4">
          <div class="d-flex align-items-center justify-content-between">
            <span class="text-muted">Avg Rating</span>
            <i data-feather="star" width="18"></i>
          </div>
          <div class="display-6 fw-bold mt-2"><%= (avgRating/total).toFixed(1) %></div>
          <div class="text-muted">Across all courses</div>
        </div>
      </div>
    </div>

    <div class="course-card p-3 p-xl-4 mt-4">
      <div class="d-flex align-items-center justify-content-between mb-3">
        <h5 class="mb-0">Quick Actions</h5>
      </div>
      <div class="d-flex flex-wrap gap-2">
        <a href="/instructor/courseList" class="btn btn-outline-primary">
          <i data-feather="grid" width="16"></i> Manage Courses
        </a>
        <a href="/instructor/addCourse" class="btn btn-outline-secondary">
          <i data-feather="plus-square" width="16"></i> Create Course
        </a>
      </div>
    </div>
  </main>




  <%- include('partial/_footer') %>
</body>

</html>