<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>EduFlex • Register</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script defer src="https://unpkg.com/feather-icons"></script>

  <link rel="stylesheet" href="/style/home.css" />
  <link rel="stylesheet" href="/style/login.css" />
</head>

<body>
  <%- include('partial/_nav') %>
  <% error = {} %>
  <% if (errors) { %>
  <% error = getError(errors) %>
  <% } %>
  <main class="auth-wrap py-5">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6 col-xl-5">
          <div class="auth-card p-4 p-lg-5">
            <div class="text-center mb-4">
              <div class="brand-mark mx-auto mb-3"></div>
              <h1 class="fw-bold mb-1">Create your account</h1>
              <p class="text-secondary mb-0">Join EduFlex and start learning today.</p>
            </div>

            <form method="post" action="/register">

              <div class="mb-3">
                <label class="form-label">Full name</label>
                <div class="input-group input-group-lg <%= error.fullname ? 'is-invalid' : ''  %>">
                  <span class="input-group-text"><i data-feather="user" width="18"></i></span>
                  <input type="text" name="full_name" class="form-control " />
                </div>
                <div class="invalid-feedback">
                  <%= error.fullname %>
                </div>
              </div>

              <div class="mb-3">
                <label class="form-label">User Name</label>
                <div class="input-group input-group-lg <%= error.username ? 'is-invalid' : '' %>">
                  <span class="input-group-text"><i data-feather="user" width="18"></i></span>
                  <input type="text" name="username" class="form-control" />
                </div>
                <div class="invalid-feedback"><%= error.username %></div>
              </div>

              <div class="mb-3">
                <label class="form-label">Email</label>
                <div class="input-group input-group-lg <%= error.email ? 'is-invalid' : '' %>">
                  <span class="input-group-text"><i data-feather="mail" width="18"></i></span>
                  <input type="email" name="email" class="form-control" />
                </div>
                <div class="invalid-feedback"><%= error.email %></div>
              </div>

              <div class="mb-3">
                <label class="form-label">Password</label>
                <div class="input-group input-group-lg <%= error.pass ? 'is-invalid' : '' %>">
                  <span class="input-group-text"><i data-feather="lock" width="18"></i></span>
                  <input type="password" id="password" name="password" class="form-control" />
                </div>
                <div class="invalid-feedback"><%= error.pass %></div>
              </div>

              <div class="mb-3">
                <label class="form-label">Confirm password</label>
                <div class="input-group input-group-lg">
                  <span class="input-group-text"><i data-feather="lock" width="18"></i></span>
                  <input type="password" id="confirmPassword" name="confirmPassword" class="form-control" />
                </div>
              </div>

              <div class="d-flex justify-content-between align-items-center mb-4">
                <div class="form-check">
                  <input class="form-check-input" type="checkbox" id="agree" />
                  <label class="form-check-label" for="agree">
                    I agree to the
                    <a href="#">Terms</a>
                    and
                    <a href="#">Privacy Policy</a>
                  </label>
                </div>
              </div>

              <button class="btn btn-primary btn-lg w-100 mb-3" type="submit">
                Create account
              </button>

              <div class="text-center small">
                Already have an account?
                <a href="/login" class="fw-semibold">Login</a>
              </div>
            </form>
          </div>

        </div>
      </div>
    </div>
  </main>

  <%- include('partial/_footer') %>
</body>

</html>