<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= course.title %></title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script defer src="https://unpkg.com/feather-icons"></script>

  <link rel="stylesheet" href="/style/home.css" />
  <link rel="stylesheet" href="/style/lesson.css" />
</head>

<body>

  <header class="ls-topbar border-bottom">
    <div class="container-fluid">
      <div class="d-flex align-items-center justify-content-between gap-2 py-2">
        <button class="btn btn-light d-lg-none" type="button" data-bs-toggle="collapse" data-bs-target="#lsSidebar" aria-controls="lsSidebar" aria-expanded="false" aria-label="Toggle sidebar">
          <i data-feather="menu"></i>
        </button>

        <h5 class="mb-0 text-truncate fw-semibold"><%= course.title %></h5>

        <div class="d-flex gap-2">
          <a href="/myCourse/myLesson/finish/<%= firstIncomplete.LessonProgress.id %>" class="btn btn-primary">
            Finish
            <i data-feather="arrow-right" class="ms-1"></i>
          </a>
        </div>
      </div>
    </div>
  </header>

  <main class="ls-main">
    <div class="container-fluid">
      <div class="row g-0">
        <aside class="col-12 col-lg-3 col-xxl-2 border-end">
          <div id="lsSidebar" class="collapse d-lg-block">
            <div class="ls-sidebar p-3">
              <a href="/myCourse/<%= user.id %>" class="ls-back link-dark d-inline-flex align-items-center mb-3">
                <i data-feather="arrow-left" class="me-2" width="18"></i>
                <span class="fw-semibold">My Courses</span>
              </a>

              <h6 class="fw-bold lh-sm mb-2"><%= course.title %></h6>
              <div class="mb-3">
                <div class="small text-secondary mb-1"><%= getPercent(totalCompleate/enrol.Lessons.length) %> % Complete</div>
                <div class="progress ls-progress">
                  <div class="progress-bar" style="width: <%= getPercent(totalCompleate/enrol.Lessons.length) %>%"></div>
                </div>
              </div>

              <nav class="ls-outline">
                <% let foundCurrent = false; %>
                <% enrol.Lessons.forEach(el => { %>
                <% if (el.LessonProgress.isCompleted) { %>
                <div class="ls-outline-item">
                  <div class="ls-outline-title">Section <%= el.section_order %>: <%= el.section_title %></div>
                  <div class="ls-outline-meta">
                    <i data-feather="check-square" width="16" class="me-1"></i>
                    <%= el.duration %> minute
                  </div>
                </div>
                <% }else{ %>
                <div class="ls-outline-item <%= !foundCurrent? 'current' : '' %>">
                  <div class="ls-outline-title">Section <%= el.section_order %>: <%= el.section_title %></div>
                  <div class="ls-outline-meta">
                    <i data-feather="square" width="16" class="me-1"></i>
                    <%= el.duration %> minute
                  </div>
                </div>
                <%  foundCurrent = true; %>
                <% } %>
                <% }) %>
              </nav>
            </div>
          </div>
        </aside>

        <section class="col-12 col-lg-9 col-xxl-10">
          <div class="ls-content p-3 p-lg-4">
            <div class="ls-player mb-4 ratio ratio-16x9">
              <iframe src="<%= toEmbedUrl(firstIncomplete.video_url) %>" frameborder="0" allowfullscreen>
              </iframe>
            </div>
            <div class="d-flex align-items-center justify-content-between flex-wrap gap-2 mb-3">
              <h4 class="mb-0 fw-semibold">Section <%= firstIncomplete.section_order %> • <%= firstIncomplete.section_title %></h4>
              <span class="text-secondary small">Last updated: <%=formatDate(firstIncomplete.LessonProgress.updatedAt) %></span>
            </div>

            <p class="text-secondary mb-4">
              <%= firstIncomplete.title %>
            </p>

            <div class="row g-3">
              <div class="col-md-6">
                <div class="ls-res card h-100">
                  <div class="card-body">
                    <h6 class="fw-semibold mb-2">
                      <i data-feather="file-text" class="me-2" width="16"></i>
                      Materi PDF
                    </h6>
                    <p class="text-secondary small mb-3">Ringkasan <%= firstIncomplete.title %>.</p>
                    <a href="/files/<%= firstIncomplete.link_materi %>" download class="btn btn-outline-secondary btn-sm">Download</a>
                  </div>
                </div>
              </div>
              <div class="col-md-6">
                <div class="ls-res card h-100">
                  <div class="card-body">
                    <h6 class="fw-semibold mb-2">
                      <i data-feather="code" class="me-2" width="16"></i>
                      Source Code
                    </h6>
                    <p class="text-secondary small mb-3">Contoh implementasi state & context.</p>
                    <a href="#" class="btn btn-outline-secondary btn-sm">Download</a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>
      </div>
    </div>
  </main>

  <%- include('partial/_footer') %>
</body>

</html>