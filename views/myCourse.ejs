<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>My Course</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script defer src="https://unpkg.com/feather-icons"></script>

  <link rel="stylesheet" href="/style/home.css" />
  <link rel="stylesheet" href="/style/my-course.css" />
</head>

<body class="d-flex flex-column min-vh-100">
  <main class="flex-grow-1">

    <%- include('partial/_nav') %>
    <section class="mc-hero py-5 border-bottom">
      <div class="container">
        <div class="row align-items-center gy-3">
          <div class="col-lg-8">
            <div class="d-flex align-items-center gap-3 mb-2">
              <div class="brand-mark d-inline-flex align-items-center justify-content-center">
                <i data-feather="book-open" class="text-white" width="18" height="18"></i>
              </div>
              <h1 class="fw-bold mb-0">My Courses</h1>
            </div>
            <p class="text-secondary mb-0">
              Lanjutkan belajar di kursus yang kamu ikuti. Semangat!
            </p>
          </div>
          <div class="col-lg-4 text-lg-end">
            <a href="/course" class="btn btn-primary">Browse more courses</a>
          </div>
        </div>
      </div>
    </section>
    <% if (enrollmentUnCompleate.length > 0) { %>
    <section class="py-4">
      <div class="container">
        <div class="d-flex align-items-center justify-content-between mb-3">
          <h4 class="fw-semibold mb-0">In Progress</h4>
        </div>

        <div class="row g-4">
          <% enrollmentUnCompleate.forEach(el => { %>
          <div class="col-12 col-md-6 col-xl-4">
            <div class="mc-card h-100">
              <div class="position-relative">
                <div class="mc-thumb">
                  <img src="<%= el.Course.image %>" alt="<%= el.Course.title %>" class="w-100 h-100 object-fit-cover rounded">
                </div>
                <span class="mc-badge badge text-bg-light"><%= el.Course.Category.name %></span>
              </div>
              <div class="mc-body p-3 p-lg-3">
                <h5 class="mc-title fw-semibold mb-1"><%= el.Course.title %></h5>
                <div class="mc-meta mb-2">by <%= el.Course.Instructor.User.full_name %></div>

                <div class="d-flex justify-content-between small text-secondary mb-2">
                  <span><%= el.Lessons.length %>/<%= el.Course.Lessons.length %> lessons</span>
                  <span><%= getPercent(el.Lessons.length/el.Course.Lessons.length) %>%</span>
                </div>
                <div class="mc-progress mb-3"><span style="width: <%= getPercent(el.Lessons.length/el.Course.Lessons.length) %>%"></span></div>

                <div class="mc-actions d-flex justify-content-between align-items-center">
                  <a href="/myCourse/myLesson/<%= el.id %>" class="btn btn-primary">Continue</a>
                  <span class="small text-secondary">Last active: <%= formatDate(el.last_activity_at) %></span>
                </div>
              </div>
            </div>
          </div>
          <% }) %>

        </div>

      </div>
    </section>
    <% } %>

    <% if (enrollmentCompleate.length > 0) { %>
    <section class="py-4 border-top">
      <div class="container">
        <div class="d-flex align-items-center justify-content-between mb-3">
          <h4 class="fw-semibold mb-0">Completed</h4>
        </div>

        <div class="row g-4">
          <% enrollmentCompleate.forEach(el => { %>
          <div class="col-12 col-md-6 col-xl-4">
            <div class="mc-card h-100">
              <div class="position-relative">
                <div class="mc-thumb">
                  <img src="<%= el.Course.image %>" alt="<%= el.Course.title %>" class="w-100 h-100 object-fit-cover rounded">
                </div>
                <span class="mc-badge badge text-bg-light"><%= el.Course.Category.name %></span>
              </div>
              <div class="mc-body p-3 p-lg-3">
                <h5 class="mc-title fw-semibold mb-1"><%= el.Course.title %></h5>
                <div class="mc-meta mb-2">by <%= el.Course.Instructor.User.full_name %></div>

                <div class="d-flex justify-content-between small text-secondary mb-2">
                  <span><%= el.Lessons.length %>/<%= el.Lessons.length %> lessons</span>
                  <span>100%</span>
                </div>
                <div class="mc-progress mb-3"><span style="width: 100%"></span></div>

                <div class="mc-actions d-flex justify-content-between align-items-center">
                  <a href="/myCourse/review/<%= el.id %>" class="btn btn-outline-secondary">Review</a>
                  <span class="small text-secondary">Completed on: <%= formatDate(el.completed_at) %></span>
                </div>
              </div>
            </div>
          </div>

          <% }) %>

        </div>
      </div>
    </section>
    <% } %>
  </main>
  <% if (msg) { %>
  <div class="position-fixed top-0 end-0 p-3" style="z-index:1080">
    <div class="toast align-items-center text-bg-success border-0 show" role="alert" aria-live="assertive" aria-atomic="true">
      <div class="d-flex">
        <div class="toast-body">
          <%= msg %>
        </div>
        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
      </div>
    </div>
  </div>
  <% } %>
  <%- include('partial/_footer') %>
</body>

</html>